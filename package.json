{
  "name": "universal-filter-monorepo",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "pnpm --filter universal-sheet build",
    "lint": "pnpm --filter universal-sheet lint",
    "test": "pnpm --filter universal-sheet test",
    "test:watch": "pnpm --filter universal-sheet test:watch",
    "test:coverage": "pnpm --filter universal-sheet test:coverage",
    "typecheck": "pnpm --filter universal-sheet typecheck",
    "playground": "pnpm --filter playground dev",
    "playground:build": "pnpm --filter playground build"
  },
  "devDependencies": {
    "shadcn": "^3.4.2",
    "eslint": "^8.56.0"
  }
}
